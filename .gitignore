# Zola build output — regenerated by `zola build`, never edited by hand.
/public

# Rust build output for the zk2data tool (tools/zk2data/).
/tools/zk2data/target

# Git worktrees live here (see CLAUDE.md). Never tracked.
/.worktrees

# Vim swap/backup files (the site is meant to be edited over SSH in vi).
*.swp
*.swo
*~
